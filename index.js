/**
 * Root entry point for VULU app
 * Uses React Navigation (migrated from expo-router)
 */

import { registerRootComponent } from 'expo';
import App from './App';

// Register the main App component
registerRootComponent(App);

